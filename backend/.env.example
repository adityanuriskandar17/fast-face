DATABASE_URL=postgresql+asyncpg://bosani:1234567890@localhost:5432/latihan_face
FACE_MATCH_THRESHOLD=0.5
LIVENESS_THRESHOLD=0.7
# "auto" (default) uses CUDA when a GPU is actually usable, CPU otherwise -- same install
# works unchanged on either kind of machine. Set explicitly (CPUExecutionProvider /
# CUDAExecutionProvider) only to force one regardless of what's detected.
INSIGHTFACE_PROVIDER=auto
INSIGHTFACE_DET_SIZE=320
